# OmniLife UAT API credentials
# Used server-side by api/omnilife/[...path].ts (Vercel serverless function).
# These are never exposed to the browser.

OMNILIFE_BASE_URL=https://uat.omnilife.com.au/api/4
OMNILIFE_USERNAME=FinuraDigital
OMNILIFE_PASSWORD=16HahIceQ42WpmurCIdP
