# OmniLife UAT API credentials
# Used by src/services/omnilifeApi.ts to load occupations and other reference data.
#
# ⚠️ These values are embedded into the compiled JS bundle because Vite exposes
# VITE_* variables to the client. Only use UAT credentials here. For production,
# proxy the API through a backend that holds the credentials server-side.

VITE_OMNILIFE_BASE_URL=https://uat.omnilife.com.au/API/4
VITE_OMNILIFE_USERNAME=FinuraDigital
VITE_OMNILIFE_PASSWORD=16HahIceQ42WpmurCIdP
